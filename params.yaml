# Chon thuat toan de huan luyen cho Bonus 2.
# Doi model_type giua cac lan chay de so sanh ket qua tren MLflow/DagsHub.
model_type: random_forest

random_forest:
  n_estimators: 200
  max_depth: 16
  min_samples_split: 2

gradient_boosting:
  n_estimators: 100
  learning_rate: 0.1
  max_depth: 3

logistic_regression:
  C: 1.0
  max_iter: 1000
